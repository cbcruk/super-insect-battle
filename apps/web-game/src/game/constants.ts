export const GAME_WIDTH = 800
export const GAME_HEIGHT = 600
export const TILE_SIZE = 32
export const GRID_WIDTH = Math.floor(GAME_WIDTH / TILE_SIZE)
export const GRID_HEIGHT = Math.floor(GAME_HEIGHT / TILE_SIZE)
