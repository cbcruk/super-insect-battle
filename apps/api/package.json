{
  "name": "@super-insect-battle/api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply super-insect-battle --local",
    "db:migrate:remote": "wrangler d1 migrations apply super-insect-battle --remote"
  },
  "dependencies": {
    "@super-insect-battle/engine": "workspace:*",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.107.0"
  }
}
