{
  "name": "@insect-battle/cli-core",
  "version": "0.1.0",
  "description": "슈퍼곤충대전 CLI 코어 라이브러리",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "node --import tsx scripts/generate-rooms.ts",
    "build": "pnpm generate && tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:run": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@insect-battle/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5",
    "yaml": "^2.7.0"
  }
}
