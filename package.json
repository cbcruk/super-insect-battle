{
  "name": "super-insect-battle",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "simulator": "pnpm --filter @super-insect-battle/simulator start",
    "dev:web": "pnpm --filter @super-insect-battle/web dev",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.7.4"
  }
}
